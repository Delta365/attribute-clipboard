{
  "name": "attribute-clipboard",
  "version": "0.1.0",
  "description": "Figma plugin: a persistent clipboard of frame attributes (fills, strokes, effects, radius, opacity, blend mode) that can be reapplied to any frame.",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "rasterize-assets": "node assets/rasterize.mjs"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.108.0",
    "@resvg/resvg-js": "^2.6.2",
    "esbuild": "^0.24.0",
    "typescript": "^5.5.4"
  }
}
